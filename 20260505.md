# Arbeitsbericht

- Name: Mate Szvercseg 
- Datum: 05.05.2026
- Thema: Aufgaben zu sh bash scripting
- Fach: SYTB
- Klasse: 3AHITS


## Übersicht
- [zur Aufgabenstellung](https://www.franzmatejka.at/htl/doc/SYTB_3/15_if_ue.html)


## Übung (Maximum)

Shellscript 

```bash
#!/bin/bash
 
x=$1 
y=$2
 
if ((x > y))
then 
echo "x > y"
else
echo "y > x"
fi
```

Terminal
```bash
┌──(kali㉿kali)-[~/20260505]

└─$ chmod +x maximum.sh
 
┌──(kali㉿kali)-[~/20260505]

└─$ ./maximum.sh 2 3 

y > x

┌──(kali㉿kali)-[~/20260505]

└─$ ./maximum.sh 3 2 

x > y
 
```

## Übung (Un-/Gerade)

Shellscript 

```bash
#!/bin/bash
 
a=$1
 
if ((a%2 == 0))
then 
echo "Diese Zahl ist gerade" 
else 
echo "Diese Zahl ist ungerade"
fi
```

Terminal
```bash
┌──(kali㉿kali)-[~/20260505]
└─$ ./GeradeUngerade.sh 3
Diese Zahl ist ungerade
┌──(kali㉿kali)-[~/20260505]
└─$ ./GeradeUngerade.sh 2
Diese Zahl ist gerade
```
## Übung (Directory)

Shellscript 

```bash
#!/bin/bash
 
a=$1
 
mkdir -p "$a" 
cd $a
echo "xyz" > xyz.txt
 
anzahl=$(find . -type f| wc -l)
if [ -n "$a" ]
then 
read -p "Soll ich das Directory \"$a\" (mit "$anzahl" files) löschen?  [j|n]:" antwort
fi
 
if [ "$antwort" = "j" ]
then 
cd
cd 20260505
rm -rf "$a"
fi
 
if [ "$antwort" = "n" ]
then exit 0
fi
```
Anmerkung: bei die if abfrage auf die leerzeichen aufpassen

Terminal
```bash
┌──(kali㉿kali)-[~/20260505]
└─$ ./makedir.sh xyz
Soll ich das Directory "xyz" (mit 1 files) löschen?  [j|n]:n
┌──(kali㉿kali)-[~/20260505]
└─$ ./makedir.sh xyz
Soll ich das Directory "xyz" (mit 1 files) löschen?  [j|n]:j
┌──(kali㉿kali)-[~/20260505]
└─$ ls 
GeradeUngerade.sh  makedir.sh  maximum.sh  xyz.txt
```
## Übung (number lines script)

Shellscript 

```bash
#!/bin/bash
 
file=$1
 
if [ -z "$file" ] #prüft ob string
then
nl /dev/stdin
exit 1
fi
 
if [ ! -f "$file" ] #-f prüft on datei existiert
then 
echo "datei existiert nicht"
exit 1 
fi
 
if [ ! -r  "$file" ] #-r prüft ob datei lesbar ist
then 
echo "datei nicht lesbar"
exit 1
fi
 
if [ ! -s "$file" ] #-s prüft ob datei leer ist
then 
echo "datei ist leer"
exit 1
fi
 
nl "$file" > tmp && mv tmp "$file"
 
cat "$file"
```
anmerkung stdin fall immer zuerst abfragen 
Terminal
```bash
┌──(kali㉿kali)-[~/20260505]
└─$ sort test.txt | ./number.sh 
     1       1  bbb eins
     2       2  ccc zwei
     3       3  aaa drei
┌──(kali㉿kali)-[~/20260505]
└─$ ./number.sh test.txt        
     1       1  bbb eins
     2       2  ccc zwei
     3       3  aaa drei
┌──(kali㉿kali)-[~/20260505]
└─$ ./number.sh leeresTest.txt 
datei ist leer
```
## Nicht erledigt
### Übung (Stundenplan V2)
