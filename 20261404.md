# Arbeitsbericht

- Name: Mate Szvercseg 
- Datum: 14.04.2026
- Thema: Aufgaben zu sh command substitution
- Fach: SYTB
- Klasse: 3AHITS

## Übersicht
- [Aufgabenstellung](https://www.franzmatejka.at/htl/doc/SYTB_3/07_cmdsubst_ue.html)

## Berechnenungen
shell skript: 
```bash
 #!/bin/bash
 
x=99999
y=88888
 
Z=$((x - y))
 
echo $Z
```

Terminal
```bash
┌──(kali㉿kali)-[~/20260414]
└─$ bash berechnungen.sh 
11111
```


## Anzahl Einträge in einem Verzeichnis
shell skript: ls
```bash
#!/bin/bash
 
DIR=$1
 
COUNT=$(ls "$DIR" | wc -l)
 
echo "Es sind $COUNT Einträge im directory $DIR"
```

Terminal
```bash
┌──(kali㉿kali)-[~/20260414]
└─$ bash eintraege.sh Test.txt 
Es sind 1 Einträge im directory Test.txt
```

shell skript ls | cat
```bash
#!/bin/bash
 
DIR=$1
 
COUNT=$(ls | cat "$DIR" | wc -l)
 
echo "Es sind $COUNT Einträge im directory $DIR"
```
Terminal
```bash
┌──(kali㉿kali)-[~/20260414]
└─$ bash eintraege.sh Test.txt 
Es sind 1 Einträge im directory Test.txt
```

shell skript: echo "$(ls)"

```bash
#!/bin/bash
 
DIR=$1
 
COUNT=$(echo "$(ls)" $DIR" | wc -l)
 
echo "Es sind $COUNT Einträge im directory DIR"`
```

Terminal:
```bash
echo "ls"
┌──(kali㉿kali)-[~/20260414]
└─$ bash eintraege.sh Test.txt 
Es sind 3 Einträge im directory Test.txt
```

## Tage bis zum Ball

```bash
#!/bin/bash
 
 
Target="2027-01-16"
 
now=$(date +%s) #Datum in sec für einfaches rechnen
 
Target_sec=$(date -d "$Target" +%s) #-d datum auslesen
 
diff=$(($Target_sec - $now))
 
days=$((diff / 86400))
 
echo "es sind noch "$days" Tage bis zum HTL Ball "$Target""
```

Terminal 
```bash
┌──(kali㉿kali)-[~/20260414]
└─$ bash DaysLeft.sh 
es sind noch 276 Tage bis zum HTL Ball 2027-01-16
```

## Zufälliger Satz

Wortliste auf kali bringen 

 ```bash 
┌──(kali㉿kali)-[~/20260414]
└─$ wget https://www.franzmatejka.at/htl/doc/SYTB_3/testdata/wortliste1000.txt

--2026-04-14 03:05:34--  https://www.franzmatejka.at/htl/doc/SYTB_3/testdata/wortliste1000.txt

Resolving www.franzmatejka.at (www.franzmatejka.at)... 185.229.88.64

Connecting to www.franzmatejka.at (www.franzmatejka.at)|185.229.88.64|:443... connected.

HTTP request sent, awaiting response... 200 OK

Length: 6987 (6.8K) [text/plain]

Saving to: ‘wortliste1000.txt’
 
wortliste1000.txt           100%[==========================================>]   6.82K  --.-KB/s    in 0s      
 
2026-04-14 03:05:34 (383 MB/s) - ‘wortliste1000.txt’ saved [6987/6987]
```
Shell script:
```bash
#!/bin/bash
 
Satz="" #String
COUNT=1000 #Zeilen anzahl der datei
for i in 1 2 3 4 5;
    do 
        index=$((RANDOM %COUNT + 1)) # Count begrenzt den Bereich, +1...beginnt bei sed 1
        word=$(sed -n "${index}p" wortliste1000.txt)
        Satz="$Satz $word"
    done
 
echo $Satz
```
Ausgabe:
```bash
┌──(kali㉿kali)-[~/20260414]
└─$ ls               
berechnungen.sh  eintraege.sh   Test.txt
DaysLeft.sh      RandomSatz.sh  wortliste1000.txt

┌──(kali㉿kali)-[~/20260414]
└─$ chmod +x RandomSatz.sh 

┌──(kali㉿kali)-[~/20260414]
└─$ bash RandomSatz.sh 
Platz Projekt hohen verkauft Beispiel
```

shell script: Erweiterung

```bash
#!/bin/bash
 
Satz="" #String
COUNT=$(wc -l < wortliste1000.txt) #anzahl der Zeilen
for i in 1 2 3 4 5;
    do 
        index=$((RANDOM %COUNT + 1)) # Count begrenzt den Bereich, +1...beginnt bei sed 1
        word=$(sed -n "${index}p" wortliste1000.txt)
        Satz="$Satz $word"
    done
 
echo $Satz
```

Ausgabe: 

```bash
┌──(kali㉿kali)-[~/20260414]
└─$ bash RandomSatz.sh    
spricht Werk Am Meinung mich
```

## Nicht erledigt
- Übung (dated copy V1)
- Übung (dated copy V2)
- Übung (dated copy V3)
